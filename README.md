# MATURITNÍ PROJEKT - INTERNETOVÝ BLOG

## Autor: VOJTĚCH SMUTNÝ 4.L

## Backend

```bash
# Spouští se (z root adresáře) příkazem
cd backend/ && npm start
```

node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
